{
  "name": "informed-consumer",
  "version": "1.0.0",
  "description": "Web App that allows comparison of products in different digital market places",
  "main": "index.js",
  "scripts": {
    "start": "node server",
    "server": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "npm install --prefix informed-consumer-app",
    "client": "npm start --prefix informed-consumer-app",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Krissan Veerasingam",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.3.1",
    "express": "^4.17.1",
    "express-validator": "^6.6.1",
    "mongoose": "^5.10.2",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.4"
  }
}
